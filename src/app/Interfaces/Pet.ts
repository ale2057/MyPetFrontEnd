export interface Pet {
  petId: number;
  name: string;
  breed: string;
  gender: string;
  family: string; //family=cat || familt=dog
  size: string;
  personality: string;
  health: string;
  story: string;
  city: string;
  adopted: boolean;
}
